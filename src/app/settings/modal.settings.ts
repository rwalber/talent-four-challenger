import { NgbModalOptions } from "@ng-bootstrap/ng-bootstrap";

export const modalConfigs: NgbModalOptions = {
    backdrop: 'static', 
    centered: true,
    animation: true,
    scrollable: true,
    windowClass: 'modal-holder', 
}